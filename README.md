# personal-site

# Running
To run this, ensure appropriate dependencies are installed as per [Jekyll installation guide](https://jekyllrb.com/docs/installation/) and execute:

```shell
  bundle exec jekyll serve
```
